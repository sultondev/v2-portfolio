export enum ProjectCategory {
  Web = 'web',
  Tooling = 'tooling',
  Oss = 'oss',
  Experiments = 'experiments',
}

export enum ProjectStatus {
  Shipped = 'shipped',
  InProgress = 'in-progress',
  Archived = 'archived',
}
